# deps
node_modules/

# next
frontend/.next/
frontend/out/
frontend/.turbo/

# db generada por el seed (se regenera con `npm run seed`)
backend/data/*.db
backend/data/*.db-journal
backend/data/*.db-wal
backend/data/*.db-shm

# env — la ANTHROPIC_API_KEY / AI_GATEWAY_API_KEY viven acá y NO se suben NUNCA
.env
.env.*
*.env
.env*.local
# ...pero la plantilla sin secretos SÍ se versiona (documenta las vars):
!.env.example
!backend/.env.example

# misc
*.log
.DS_Store
