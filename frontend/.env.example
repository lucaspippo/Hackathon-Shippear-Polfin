# ============================================================================
# PolFin · plantilla de entorno del frontend. Copiala a frontend/.env:
#   cp .env.example .env
# El .env real está gitignoreado. Next.js carga .env automáticamente.
# ============================================================================

# URL base de la API del backend (default: http://localhost:4000 si se omite).
NEXT_PUBLIC_API_URL=http://localhost:4000
